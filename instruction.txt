**************** link new**********************
https://www.youtube.com/watch?v=AT1uxOLsCdk

**************** link **********************

Git is a Tool that allows you version control.
Version control means Whenever code is modified, Whenever code is created. Whenever code is deleted, every timestrep,  everything  git does for you.    

--- Start ----
git --version // to check git version 
git config --global user.name "name"
git config --global user.email "email"
	git config --global user.name
	git config --global user.email

just understanding for me
       contant version
Git => afzal => 0.0.1
Git => af   => 0.0.2
Git => 560  => 0.0.3

…or create a new repository on the command line
echo "# mi_card" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/afzalsorathiya/mi_card.git
git push -u origin main

// First Time push Code to Github Basic 
	1. git init
	2. git remote add origin <repo link>
	3. git add . || add . means all files || add <filename> //  cold Stagging
	4. git commit -m "Message" // commiting
	5. git push || git push origin <branch name>
       6. Git status

Remote add    
       // change existing url 
       git remote set-url origin [url]
       -> $ git remote set-url origin https://git-repo/new-repository.git
       git remote set-url <Remote name> <URL>


       // You can check which remote url is already exit       => git remote -v
       git remote add origin <repo link>
       

Vs code - Understanding of GIT 
       - Anything you can change to file vs code below left side <branch name>* || master* <--- modified file || 
                                                                                    master + <---- ||Stagged Changes
                                          if your file is stagged after this unstagged command is
                                                 - git Restore --staged <file name> || git restore --staged .

              // After commiting 
                     Vs code below left side 1 request send to server 1| sign  

                     // All files clear 
                            than simple |||||||||||||||||| master    

Branch - 
       Create New Branch => 
              git checkout -b dev
       move one Branch to another 
              git checkout master
       Git Restore <file name>

// Check Previous commit
Git log 
Every programmer should known git.